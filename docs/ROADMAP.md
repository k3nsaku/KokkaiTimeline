# これからやること

**進捗の記録ではなく、残っている作業だけを書く。**
済んだことの経緯は git の履歴にある。決定の理由は [DECISIONS.md](DECISIONS.md)。

- 最終更新: 2026-08-06
- 現在地: **サイトは動いていて、公開の関門（性能・法務・争点語のレビュー）は全部通った。**
  **日次更新の自動化は通った**（2026-08-06、全ステップ成功・4分37秒）。
  **残るは `kokkai-timeline.com` を Pages に当てるだけ。当てた時点で公開。**

---

## 1. 日次更新を自動化する

**2026-08-06 に初回実行が全ステップ成功した。**残りは独自ドメインを当てるところだけ。
値と手順は [PIPELINE.md](PIPELINE.md)。

- [x] **GitHub にリポジトリを作って push する** — `k3nsaku/KokkaiTimeline`（public / 既定 `master`）
- [x] Variables 4つ を登録する
- [x] **Secrets 4つ を登録する**
      `R2_ACCOUNT_ID` / `R2_ACCESS_KEY_ID` / `R2_SECRET_ACCESS_KEY` / `CLOUDFLARE_API_TOKEN`
- [x] **Actions の Workflow permissions を「Read and write」にする。**
      `daily.yml` は `data/politician_ids.json` を repo に書き戻す。
      2023年2月以降に作ったリポジトリは既定が read-only で、**そのままだと最後に落ちる**
      （台帳を失うとURLが全部変わるので、わざと失敗させてある）
- [x] `workflow_dispatch` で1回流す（`all_years=false`）— run 31086294777 / 全ステップ成功
- [ ] **`kokkai-timeline.com` を Pages に当てる。当てた時点で実質公開になる**

> **★ 台帳の書き戻しはまだ一度も実行されていない。**
> 初回実行では議員IDに差分が無く（1,111件のまま）、`git push` の手前で
> `exit 0` している。**Workflow permissions を Read and write にした効果は未検証。**
> 議員が増えた日に初めて通る経路なので、そのとき最後のステップが落ちていないか見ること。

> **リポジトリは public にした。** Actions の無料枠は public なら実質無制限、
> private は月2,000分。閉会中は1回5分弱だが、会期中は取得が伸びる。

## 2. 争点語・頻出語を主機能に寄せる

方針の理由は [DECISIONS.md](DECISIONS.md) の「争点語・頻出語を主機能にする」。
**A（半角→全角）と B（2文字の全角ラテン）は実装済み。** 残りは C・D・E で、
**どれも全年DBの作り直しが要らない**ので公開を止めない。

### C. 頻出語レイヤー（`topic` とは別の第3の層）

**差別化の本体。** NDL は「語を入れたら発言が出る」まで。
**語の側から「いつ・どれだけ議論されたか」を見せる**機能は無い。

素材はほぼ揃っているので、見た目より安い:

- `build_topics.py` の `propose()` — 候補の抽出とランキングが既にある
- 同 `make_word_filter()` — **人名・役職・年号のフィルタが既にある**
- `topic_denylist.json` — 取りこぼしを1行で消す逃げ道も既にある

新規に書くのは出力形式だけ。**`data/dist/frequent.json`**（月×語の件数 + 分母）。

- **`topics.json` は82件のまま増やさない。** あれは編集方針、こちらは機械抽出。
  **ページ上でも「運営が選んだ争点語」と「機械抽出の頻出語」を明示的に分ける**
- **会派の内訳は上位N語にだけ付ける。** `topics.json` は 82語×20会派×64月で 278KB。
  同じ密度で500語やると約1.7MB。語だけなら500語×64月で数十KB。
  **分布を見たい語は限られる**ので上位だけで足りる（N は実測して決める）
- **DB再構築は不要。** ビルド時に Python で集計し、発言一覧は既存の索引に流す
- **分母（その月の発言数）で割る**

### D. 任意期間（月・会期）の抽出

**C を月粒度で持てば、ほぼタダ。** クライアントで月を足し合わせるだけ。

- **やってはいけない**: 任意期間をブラウザから `word_hit` / FTS で集計すること。
  年330万行の範囲集計は全走査になり、`ORDER BY date DESC` と同型の穴に落ちる
- **粒度は月に固定する。** 日付の自由入力は割に合わない
- 代わりに **`meeting.会期` を選べるようにする。**「第213回通常国会で急に増えた語」は
  政治的に意味のある区切りで、データは既にあり、コストは月の合算と変わらない

### E. 導線の入れ替え

トップを検索窓中心から**争点語・頻出語・急増語**中心に組み替え、検索は入口の1つに降ろす。
**既存機能は消さない。**「主にする」は情報の順序で表現する。

## 3. 小さい残件

- [ ] **議員ページの頻度推移グラフ**（議員 × 争点語）。`dist/topics.json` は
      月×**会派**しか持っていないので、出すなら集計を足す必要がある
- [ ] **ページ送りの実機確認。** keyset で実装済みだが、遅延20msを入れた計測はしていない
- [ ] **CI（push ごとの `npm run check`）。** public にしたら足すか判断する
- [ ] `/speech/<id>` の OGP。いまは全発言で共通
      （解決するには Workers が要り、「常時稼働を持たない」制約に触れる。
      共有が実際に伸びないと分かってから考える）

---

## 決めていないこと

- **アクセス解析を入れるか。** 入れるとプライバシーポリシーが重くなる
  （`operator.ts` の `analytics` を埋めることになる）
- **頻出語をどこまでの語数で出すか**（会派の内訳を付ける上位N語の N）。実測してから決める
- **ライセンス。** `LICENSE` を置いていない（＝現状は全権利留保）

---

## 未解決の不具合

### Chrome でブラウザキャッシュが壊れると全ページが落ちる（原因は未特定）

2026-08-02 に実機で発生。**原因は分かっていないが、「一度壊れると永久に直らない」
性質のほうを消した**ので、公開の関門からは外している。

症状は全ページで `SQLite: file is not a database`。Chrome の通常ウィンドウだけで起き、
シークレット・Firefox・Vivaldi では正常。**Ctrl+Shift+R では直らず**、
キャッシュ削除で直る。Vivaldi も Chromium なので**エンジンではなくプロファイル固有**。

潰した容疑者: R2 の配信（無実。冷えたキャッシュでも 206 を 49ms で正しく返す）、
年DBの中身（無実。6年ともヘッダ正常・サイズ一致）、
`accept-ranges` が CORS で公開されていないこと（**原因ではない**。配布物を読むと
この値は警告を出すだけで取得の仕方を変えていない）。

対処として `immutable` を外し（再読み込みで治るようになった）、
やり直しに `&retry=` を足してURLを変えるようにした。
配信側を壊す再現（`POISON=1`）では6年すべてが自力で復旧するが、
**実機の再発はこれでは救えなかった**＝本物の壊れ方を再現できていない。

**再発したら、キャッシュを消す前にこれを見ること:**

1. **Console に `年のDBを読み直す` が出ているか** — 出ていれば再試行は走っている
   （＝別URLでも壊れたものが返る。キャッシュ以外の要因）
2. **Network タブの `kokkai-*.db` の Size 列。** `(disk cache)` ならブラウザキャッシュ由来。
   `retry=` 付きのURLが並んでいるかも見る
3. **`cache-control` に `immutable` が残っていないか**

---

## 将来（Phase 2 以降）

Phase 1 が公開に至ってから着手する。

- **参議院の押しボタン投票**（個人別結果が公開されている）と衆議院の記名投票。
  `Bill` / `Vote` のスキーマは最初から用意しておく。
  ★**衆院の起立採決には個人別の記録が残らない**ので、
  「衆院議員の全議案賛否一覧」は原理的に作れない（[SCOPE.md](SCOPE.md)）
- 議員・政党の公式サイト RSS（RSSを持つ議員は体感2〜3割）、YouTube Data API v3（無料枠あり）
- 発言ごとの要約。**全件は予算に収まらない**ので、やるなら対象を絞る。
  法的には「意見を付加しない・原文併記・AI生成の明示」が条件
